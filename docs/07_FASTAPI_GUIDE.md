# FastAPI Guide

- Applies starting in Sprint 8 only
- Versioned API: `/api/v1`
- Use Pydantic request/response models
- Global exception handlers
- Health endpoint required
- No YOLO calls inside routers
- Dependency injection for services
- OpenAPI docs must remain valid
- This guide does not authorize API code before Sprint 8
